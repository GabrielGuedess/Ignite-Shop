import { styled } from 'styles';

export const Wrapper = styled('div', {
  display: 'flex',
  width: '100%',
  maxWidth: 'calc(100vw - ((100vw - 118.0rem) / 2))',
  minHeight: '65.6rem',
  marginLeft: 'auto',
});
